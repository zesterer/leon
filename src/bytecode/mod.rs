mod gc;

/*
pub enum Value {
    String(String),
    Number(f64),
    Bool(bool),
    Null,
    Func {
        addr: u32,
        arity: u16,
        env: Vec<Self>,
    },
    Struct(HashMap<u32, Self>),
    Ref(Rc<RefCell<Self>>),
}

pub enum Instr {
    Value(),
}
*/
