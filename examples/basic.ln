let total = 0;
let i = 0;
while i < 100 {
	i += 1;
	total += i;
};
total;

let add = |a, b| a + b;
let map = |x, f| f(x);
map(add(4, 5), |x| x * x)

#x += 5;

#let x = if true { 5 } else { 3 };

#while x > 0 {
#	x -= 1;
#};

#3 + (5 + 7 * {
#	let foo = 10;
#	foo - 9
#})
