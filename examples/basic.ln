var total = 0;
var i = 0;
while i < 100 {
    i += 1;
    total += i;
}
total;

# Functions
var add = |a, b| a + b;
var map = |x, f| f(x);
map(add(4, 5), |x| x * x);

# Strings and loops
var i = 0;
var str = "X";
while i < 10 {
	str += "Y";
	i += 1;
}
str;

# Indexing
var str = "Hello, world!";
var x = str[1];
